--> Whether a scene was played is its `status` alone (`scenes.status =
--> 'played'`). A session records no played scenes: the scenes of an evening
--> are the scenes its log entries name. The table goes with its rows.
DROP TABLE `played_scenes`;
